# Extracted CSV files from zip archives (regenerated by analysis script when needed)
# The original data exists in .zip files; extracted CSVs should not be committed

# Pattern for all extracted 1m CSV files (5m and 15m are not zipped)
*[0-9][0-9][0-9][0-9] 1m.csv
